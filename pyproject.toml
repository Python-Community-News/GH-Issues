[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "gh_issues"
version = "0.0.1"
dependencies = [
    "httpx",
    "markdown-it-py",
]

[project.optional-dependencies]
dev = [
    "pytest",
    "pytest-httpx",
    "pip-tools",
]
